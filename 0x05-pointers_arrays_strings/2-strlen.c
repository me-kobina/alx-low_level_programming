#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - function that prints the length of a string
 * @s: char variable
 * Return: len -length of s
**/

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
